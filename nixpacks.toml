# Явная сборка через bun + Node.js 22 (Nixpacks / Railway).
# Перекрывает дефолтный Node 18, который удалён из nixpkgs.

[phases.setup]
nixPkgs = ['bun', 'nodejs_22']

[phases.install]
cmds = ['bun install']

[phases.build]
cmds = ['bun run build']
